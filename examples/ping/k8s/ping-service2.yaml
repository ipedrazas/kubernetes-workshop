apiVersion: v1
kind: Service
metadata:
  name: pingsvcname2
  labels:
    name: pingservice
spec:
  type: LoadBalancer
  ports:
    # the port that this service should serve on
    - port: 8000
      targetPort: 8080
  selector:
    name: pingrcworkshop
